"""
voice.py

This module is no longer responsible for handling voice input.
(JavaScript in the browser).

The backend only receives text (via /process endpoint) and parses it
with StitchFlowAgent.
"""